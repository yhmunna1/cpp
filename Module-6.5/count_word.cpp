#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;
    getline(cin, s);
    int word = 0;
    for (int i = 0; i < s.size(); i++)
    {
        if (s[i] == ' ')
        {
                }
    }

    word += 1;

    cout << word << endl;
    return 0;
}